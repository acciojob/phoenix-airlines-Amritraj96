import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App'; // Make sure this path is correct based on your folder structure

ReactDOM.render(<App />, document.getElementById('root'));
